server:
  port: 8083

spring:
  application:
    name: service-care
  config:
    import: optional:nacos:service-care.yaml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml

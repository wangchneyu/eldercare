server:
  port: 8094

spring:
  application:
    name: service-iot
  config:
    import: optional:nacos:service-iot.yaml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
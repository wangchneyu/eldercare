spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/eldercare_iot?currentSchema=public
    username: postgres
    password: postgres
    driver-class-name: org.postgresql.Driver

mybatis-plus:
  configuration:
    map-underscore-to-camel-case: true
  global-config:
    db-config:
      id-type: assign_id

mqtt:
  broker-url: tcp://localhost:1883
  client-id: iot-service-test

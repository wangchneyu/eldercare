server:
  port: 8095

spring:
  application:
    name: service-alert
  config:
    import: optional:nacos:service-alert.yaml
  cloud:
    nacos:
      discovery:
        server-addr: 127.0.0.1:8848
      config:
        server-addr: 127.0.0.1:8848
        file-extension: yaml
